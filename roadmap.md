## Glassmorphic Regression Cleanup

| Task | Subtasks | Owner | Status |
|------|----------|-------|--------|
| Reproduce regression | 1. Run `xcodebuild … run` on iPhone 16 Pro sim.<br>2. Create checklist of every tappable element per screen.<br>3. For each failure, capture video, simulator log, and layer tree screenshot. | Wallet Dev | ☐ |
| UI hierarchy audit | 1. Use Debug View Hierarchy to inspect Register, Send, Receive, Wallet controllers individually.<br>2. Export flattened view tables showing frame, z-index, and interaction enabled flags.<br>3. Annotate which overlay view is blocking each control. | Wallet Dev | ☐ |
| Harden `GlassmorphicStyle` | 1. Add unit tests verifying `GradientBackgroundView`/blur inserts return `false` for `point(inside:with:)`.<br>2. Set `isUserInteractionEnabled = false` on all decorative layers.<br>3. Refactor `installGlassBackground()` to guard against duplicates, always insert at index 0, and expose a teardown helper.<br>4. Audit button gradient helper to ensure it uses `allowsGroupOpacity = true` and doesn’t add overlay sublayers with hit-testing. | Wallet Dev | ☐ |
| Screen-specific fixes | **Register:** ensure rotating image view has `isUserInteractionEnabled = false`; move button container to front; add snapshot test verifying target/action still fires.<br>**Send:** split blur from content so text fields/buttons sit in a separate container; confirm tap gestures reach fields; add UI automation step to focus each field.<br>**Receive:** shrink QR glass card to QR bounds, mark QR/image overlays as non-interactive, verify copy/reset IBAction wiring; add instrumentation log when buttons fire.<br>**Wallet:** adjust header height constraints to avoid overlapping first cell; add tap-hittest debug overlay to confirm touches pass through. | Wallet Dev | ☐ |
| Outlet cleanup | 1. Open `Main.storyboard`, rename `property` ➜ `propertyLabel` and `pubkey` ➜ `pubkeyLabel` in Interface Builder.<br>2. Remove bridging outlets from `ReceiveViewController` and re-run `xcodebuild` to confirm no KVC exceptions.<br>3. Add storyboard lint step (e.g., `ibtool --validate`) to CI to prevent stale outlet names. | Wallet Dev | ☐ |
| Automated tests | 1. Add XCTestCase that launches the app, performs registration (mock Torii), and asserts success alert; include screenshot attachment on failure.<br>2. Add UI test for Send flow with stubbed Torii response, asserting success alert and field reset.<br>3. Add UI test that taps Receive “Copy”, then reads `UIPasteboard.general.string` and compares to accountId.<br>4. Integrate tests into CI via `xcodebuild test` on iPhone 16 Pro sim, capturing `.xcresult`. | Wallet Dev / QA | ☐ |
| Docs & verification | 1. Update `AGENTS.md` with “Decorative layers must be non-interactive, overlays inserted behind content, storyboard outlets kept in sync.”<br>2. Document the new UI tests + how to mock Torii responses for them.<br>3. Run `pod install` if storyboard changes bump integration.<br>4. Build via `xcodebuild -workspace Example-Point.xcworkspace -scheme Example-Point -destination 'platform=iOS Simulator,name=iPhone 16 Pro,OS=26.1' CODE_SIGNING_ALLOWED=NO build` and attach log to PR.<br>5. Optionally, add a short Loom of manual smoke test for PM review. | Wallet Dev | ☐ |
